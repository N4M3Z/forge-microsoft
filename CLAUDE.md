# CLAUDE.md

> `CLAUDE.md` and `AGENTS.md` are autogenerated by `/init`. Do not edit directly.

Microsoft 365 integration — Outlook email, Todo tasks, Calendar events, Teams chat via `m365` CLI.

## Skills

Skills in `skills/*/SKILL.md`: EdgeBrowser, M365. M365 provides Outlook email, Todo task management, Calendar events, and Teams chat operations. See `rules/` for m365 CLI auth and pitfalls.

## Install / Verify

```bash
make install          # deploy skills (Claude, Gemini, Codex, OpenCode)
make verify           # check skills deployed across all providers
make clean            # remove previously installed skills
make test             # validate-module convention checks
make lint             # schema + shell linting
```

### Tool

[m365 CLI](https://pnp.github.io/cli-microsoft365/) — install via `npm install -g @pnp/cli-microsoft365`.

## Architecture

```
hooks/
    hooks.json         empty — no active hooks
rules/
    AuthM365.md        m365 CLI authentication flow
    PitfallsM365.md    confirmed CLI runtime limitations
skills/
    EdgeBrowser/       Edge browser automation
    M365/              Microsoft 365 operations (Outlook, Todo, Calendar, Teams)
lib/                   git submodule -> forge-lib (Rust binaries for deployment + validation)
module.yaml            module metadata (name, version)
.claude-plugin/        plugin.json manifest for Claude Code plugin discovery
```

## Conventions

### Git

Conventional Commits: `type: description`. Lowercase, no trailing period, no scope.

Types: `feat`, `fix`, `docs`
