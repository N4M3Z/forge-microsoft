# forge-microsoft defaults
# Override: create config.yaml (gitignored) with only the fields you want to change.

skills:
    claude:
        EdgeBrowser:
        ExplainPowerShell:
    gemini:
        EdgeBrowser:
        ExplainPowerShell:
    codex:
        EdgeBrowser:
        ExplainPowerShell:
    opencode:
        EdgeBrowser:
        ExplainPowerShell:

agents:
    MicrosoftAdvisor:
        model: fast
        tools: Read, Grep, Glob, Bash

providers:
    claude:
        fast: claude-sonnet-4-6
        strong: claude-opus-4-6
    gemini:
        fast: gemini-2.0-flash
        strong: gemini-2.5-pro
    codex:
        fast: o4-mini
        strong: o4-mini
    opencode:
        fast: claude-sonnet-4-6
        strong: claude-opus-4-6
